# ============================================================
# requirements.txt  —  Linux / GitHub Codespaces / Docker
# Python 3.12  (code syntax is 3.8-compatible, runs on 3.8–3.12)
#
# Install:  pip install -r requirements.txt
#
# Windows local dev: also run  pip install -r requirements_win.txt
# ============================================================

# ── Web framework ──────────────────────────────────────────
fastapi==0.115.8
uvicorn[standard]==0.33.0      # [standard] pulls in websockets, httptools, uvloop
starlette==0.44.0
Jinja2==3.1.5
python-multipart==0.0.20       # required for Form() / file upload
pydantic==2.10.6
pydantic-settings==2.7.0       # optional but useful for env-var config

# ── Data ───────────────────────────────────────────────────
pandas==2.2.3                  # was 2.0.3 — no cp312 wheel, caused the build failure
numpy==1.26.4                  # was 1.24.4 — no cp312 wheel, caused the build failure
SQLAlchemy==2.0.36             # used in database/db_manager.py

# ── HTTP / Orion ───────────────────────────────────────────
requests==2.31.0
urllib3==1.26.18               # pinned to 1.x — orionsdk 0.4.0 not compatible with urllib3 2.x
orionsdk==0.4.0

# ── Scheduling ─────────────────────────────────────────────
APScheduler==3.10.4            # 3.11+ has regression on older Python; 3.10.4 stable on 3.8–3.12

# ── SSH / Networking ───────────────────────────────────────
netmiko==4.3.0                 # upgraded from 3.1.1 — fixes Comware/EOS issues
                               # ⚠ BREAKING import path change (see note below)
paramiko==3.4.0
scp==0.14.5
websockets==13.1               # used by websocket_server_ds.py + uvicorn[standard]
textfsm==1.1.3

# ── Async / Files ──────────────────────────────────────────
aiofiles==23.2.1               # 24.x dropped Python 3.8; 23.2.1 works on 3.8–3.12
anyio==4.3.0
python-dotenv==1.0.1
watchfiles==0.21.0             # 0.22+ dropped Python 3.8; 0.21.0 has cp38–cp312 wheels

# ── Logging ────────────────────────────────────────────────
concurrent-log-handler==0.9.28 # ConcurrentRotatingFileHandler used in mainconfig.py

# ── Support ────────────────────────────────────────────────
certifi==2024.2.2
cryptography==42.0.5           # updated from 41.x for CVE patches
python-dateutil==2.9.0.post0
six==1.16.0

# ============================================================
# ⚠  netmiko v4 breaking change — update these imports:
#
#   OLD (v3):  from netmiko.ssh_exception import NetMikoTimeoutException
#              from netmiko.ssh_exception import AuthenticationException
#   NEW (v4):  from netmiko.exceptions import NetMikoTimeoutException
#              from netmiko.exceptions import NetMikoAuthenticationException
#
#   Files to update:
#     utils/network.py        line 5
#     utils/fastapi_mymodule.py  lines 13-14
# ============================================================